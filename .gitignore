.DS_Store
*.tmp
*.bkp
.env

# Música comercial (copyright) — nunca versionar/distribuir no site público
/Faith No More -*.mp3
/Led Zeppelin -*.mp3
/Metallica -*.mp3
/Rush -*.mp3
/The Cure -*.mp3
/The Doors -*.mp3
/Natiruts -*.mp3
wavs/
clipes/
videos/
# Render outputs (YT/TikTok) — não servidos pelo site; inchavam o artifact do Pages e faziam o deploy falhar no syncing_files
shorts/
clipes_suno/
*.bak
*.bak-*
estado_shorts.json
logs/*.log
stats_radio.json
podcast/

# Locucoes dos locutores IA: regravadas de hora em hora e regravadas de novo.
# Nao sao servidas pelo site (nenhum html referencia locucoes/) — vao para a
# radio, pelo disco. Versionadas, geraram 315 MB de historico morto. O
# manifest.json continua versionado: e texto, e e o registro do que foi falado.
locucoes/*.mp3

# Estado que muda a cada musica — versionar so gera commit sem conteudo
nowplaying.json

# Arquivo morto do expira_sazonais.sh — fica no disco, nao no git
sazonais_fora_de_epoca/
